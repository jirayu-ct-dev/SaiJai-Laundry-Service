-----

# 🧺 SaiJai Laundry Service (ระบบจัดการร้านซักรีด "ใส่ใจ")

**"SaiJai Laundry Service"** เป็นระบบจัดการร้านซักรีดแบบ Full-Stack ที่สร้างขึ้นเพื่อตอบโจทย์การใช้งานจริงในร้านซักรีดสมัยใหม่ โปรเจกต์นี้พัฒนาระบบทั้งหมดตั้งแต่หลังบ้าน (Admin Panel) ไปจนถึงหน้าบ้านที่เชื่อมต่อกับผู้ใช้งานโดยตรงผ่าน **Line LIFF (Line Front-end Framework)**

เป้าหมายของโปรเจกต์คือการเพิ่มประสิทธิภาพการทำงานของพนักงาน, สร้างประสบการณ์ที่ดีให้กับลูกค้าผ่านการแจ้งเตือนและติดตามสถานะ, และช่วยให้ผู้ดูแลระบบสามารถจัดการแพ็คเกจและราคาได้อย่างง่ายดาย

## ✨ Key Features (คุณสมบัติหลัก)

ระบบถูกแบ่งการเข้าถึงตามบทบาท (Role-Based Access) 3 ส่วนหลัก:

### 👤 1. Customer (สำหรับลูกค้า)

  * **Login & Register:** เข้าสู่ระบบและสมัครสมาชิกอัตโนมัติผ่าน **Line LIFF**
  * **Real-time Order Tracking:** ติดตามสถานะผ้าของตนเองได้แบบเรียลไทม์ (เช็คผ้า, ซัก, อบ/ตาก, รีด, พร้อมรับ)
  * **Line Notifications:** รับการแจ้งเตือนผ่าน Line OA เมื่อสถานะออเดอร์มีการเปลี่ยนแปลง
  * **Package Management:** ตรวจสอบแพ็คเกจรายเดือนคงเหลือ (เครดิต, วันหมดอายุ)
  * **Storefront Browsing:** ดูรายการบริการและราคาหน้าร้าน (สำหรับลูกค้าที่ไม่ได้ใช้แพ็คเกจ)

### 🧑‍🏭 2. Employee (สำหรับพนักงาน)

  * **Employee LIFF:** หน้าจอการทำงานของพนักงานที่ออกแบบมาสำหรับมือถือผ่าน LIFF
  * **Task Dashboard:** ดูรายการงานที่ต้องทำในแต่ละวัน
  * **QR Code Workflow:**
    1.  **ผูกออเดอร์:** สแกน QR Code บนตะกร้าเพื่อผูกกับออเดอร์ใหม่ (ทั้งแบบแพ็คเกจและหน้าร้าน)
    2.  **อัปเดตสถานะ:** สแกน QR Code เดิมซ้ำเพื่ออัปเดตสถานะของตะกร้านั้นๆ (เช่น สแกนที่จุดซัก -\> สถานะเปลี่ยนเป็น "กำลังซัก")
  * **Image Proof:** ถ่ายรูปผ้าเป็นหลักฐาน (เช่น พบผ้าชำรุดก่อนซัก) และอัปโหลดเข้าระบบ (ผ่าน Cloudinary)

### 👑 3. Admin (สำหรับผู้ดูแลระบบ)

  * **Web Dashboard:** หน้าแผงควบคุมสำหรับจัดการระบบผ่าน Web Browser
  * **User Management:** จัดการผู้ใช้และกำหนดสิทธิ์ (Admin, Employee, Customer)
  * **Package Management (CRUD):** สร้าง, ลบ, แก้ไข แพ็คเกจรายเดือน (กำหนดราคา, เครดิต, วันหมดอายุ, บริการรับส่ง)
  * **Payment Approval:** ตรวจสอบสลิปที่ลูกค้าส่งมา และกดยืนยันเพื่อเปิดใช้งานแพ็คเกจ (Activate `UserPackage`)
  * **Storefront Price Management:**
      * จัดการ "บริการ" (เช่น ซักอบรีด, รีดอย่างเดียว)
      * จัดการ "ประเภทผ้า" (เช่น เสื้อยืด, ผ้าห่ม)
      * กำหนด "ราคาสุทธิ" (เช่น เสื้อยืด + รีดอย่างเดียว = 15 บาท)
  * **System Overview:** ดูภาพรวมออเดอร์และรายได้ทั้งหมด

## 🛠️ Tech Stack (เทคโนโลยีที่ใช้)

| Category | Technology | Description |
| :--- | :--- | :--- |
| **Core Framework** | **Nuxt 3** (Vue 3) | ใช้ในโหมด Full-stack (ทั้ง Frontend และ Backend API) |
| **Language** | **TypeScript** | เพิ่มความเสถียรและความถูกต้องของ Code |
| **Runtime** | **Bun** | JavaScript Runtime/Bundler/Package Manager ที่รวดเร็ว |
| **UI/Styling** | **Tailwind CSS** | Utility-first CSS framework |
| **UI Components** | **Nuxt UI** | ชุด UI Components สำเร็จรูปสำหรับ Nuxt |
| **Database** | **PostgreSQL** | ฐานข้อมูลหลัก (ทดสอบด้วย DBeaver, โฮสต์บน Supabase) |
| **ORM** | **Prisma** | Next-generation ORM สำหรับ TypeScript และ Node.js |
| **Authentication** | **Better Auth** | ระบบจัดการ Authentication (Social, Email/Pass, Magic Link) |
| **File Storage** | **Cloudinary** | บริการจัดการและเก็บไฟล์รูปภาพ (เช่น รูปสลิป, รูปหลักฐานผ้า) |
| **Platform** | **Line (LIFF & OA)** | เชื่อมต่อกับผู้ใช้ผ่าน LIFF และส่งการแจ้งเตือนผ่าน Messaging API |
| **DevOps** | **Docker** | ใช้สำหรับจำลองสภาพแวดล้อม (Local PostgreSQL) |

## 📐 System Architecture (สถาปัตยกรรม)

โปรเจกต์นี้ใช้ประโยชน์จาก Nuxt 3 ในการเป็น Monorepo แบบ Full-stack:

  * **`Nuxt Client (Frontend)`**:

      * รันบน Browser ของผู้ใช้ (ทั้งบน Web และใน Line LIFF)
      * จัดการ UI, State Management, และการเรียก API
      * เป็นส่วนที่ผู้ใช้ (Admin, Employee, Customer) โต้ตอบด้วยโดยตรง

  * **`Nuxt Server (Backend)`**:

      * รันบน Server (e.g., Vercel, Node.js server)
      * ให้บริการ API Routes (`/server/api`)
      * จัดการ Business Logic ทั้งหมด เช่น การอนุมัติแพ็คเกจ, การอัปเดตสถานะ
      * เชื่อมต่อกับฐานข้อมูล (PostgreSQL) ผ่าน **Prisma**
      * ติดต่อกับ External APIs (Cloudinary, Line Messaging API) อย่างปลอดภัย

## 🚀 Getting Started (การติดตั้งและรันโปรเจกต์)

### 1\. Prerequisites (สิ่งที่ต้องมี)

  * [Bun](https://bun.sh/) (v1.0.x หรือสูงกว่า)
  * [Docker](https://www.docker.com/) (สำหรับรัน PostgreSQL ใน Local)
  * บัญชี Supabase (สำหรับ Production DB), Cloudinary, และ Line Developers

### 2\. Clone Project

```bash
git clone https://github.com/YOUR_USERNAME/saijai-laundry-service.git
cd saijai-laundry-service
```

### 3\. Install Dependencies

```bash
bun install
```

### 4\. Environment Variables (ตั้งค่า .env)

สร้างไฟล์ `.env` จาก `.env.example` และกรอก Keys ที่จำเป็นทั้งหมด:

```.env.example
# Database (Supabase/Local)
# ใช้ URL จาก Supabase หรือ Local Docker
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Better Auth
# สร้าง Secret Key (e.g., openssl rand -base64 32)
AUTH_SECRET="YOUR_STRONG_AUTH_SECRET_KEY"
AUTH_ORIGIN="http://localhost:3000"

# Line Platform
LINE_CHANNEL_ACCESS_TOKEN="YOUR_LINE_OA_CHANNEL_ACCESS_TOKEN"
LINE_CHANNEL_SECRET="YOUR_LINE_OA_CHANNEL_SECRET"
NUXT_PUBLIC_LINE_LIFF_ID="YOUR_LIFF_ID"

# Cloudinary
NUXT_PUBLIC_CLOUDINARY_CLOUD_NAME="YOUR_CLOUD_NAME"
CLOUDINARY_API_KEY="YOUR_API_KEY"
CLOUDINARY_API_SECRET="YOUR_API_SECRET"

# Base URL
NUXT_PUBLIC_BASE_URL="http://localhost:3000"
```

### 5\. Database Setup (ตั้งค่าฐานข้อมูล)

รันคำสั่งนี้เพื่อสร้างตารางทั้งหมดในฐานข้อมูล (ตาม Schema ใน `prisma/schema.prisma`)

```bash
bunx prisma migrate dev
```

(Optional) หากคุณมีไฟล์ Seed (e.g., `prisma/seed.ts`) ให้รัน:

```bash
bunx prisma db seed
```

### 6\. Run the Project (รันโปรเจกต์)

```bash
bun run dev
```

เปิด [http://localhost:3000](https://www.google.com/search?q=http://localhost:3000) เพื่อดูโปรเจกต์
